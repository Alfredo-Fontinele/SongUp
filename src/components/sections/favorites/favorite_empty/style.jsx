import styled from "styled-components";

export const FavoriteEmpty = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    max-width: 1000px;

    h3 {
        font-size: 24px;
        color: var(--primary);
    }
`